pipeline {
  agent any
  stages {
    stage('Git Pull'){
      node('node name'){
        steps {
          sh 'ls'
        }
      }
    }
    stage('Build') {
      steps { 
        sh 'ls' 
      }
    }
  }
}